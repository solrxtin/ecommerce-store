{
  "name": "mern-crash-course",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cross-env NODE_ENV=development npx nodemon backend/server.js",
    "build": "npm install && npm install --prefix frontend/vite-project && npm run build --prefix frontend/vite-project",
    "start": "cross-env NODE_ENV=production node backend/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "mongoose": "^8.7.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
